{
  "name": "quartet-chooser",
  "private": true,
  "description": "Quartet Chooser",
  "author": "Jason Sundram",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "checklist": "node scripts/make-checklist.mjs",
    "icons": "node scripts/make-icons.mjs",
    "og": "node scripts/make-og.mjs",
    "serve": "npx serve dist",
    "test": "node --test \"test/*.test.mjs\"",
    "test:update": "node scripts/build.mjs && node scripts/update-fixtures.mjs"
  },
  "dependencies": {
    "esbuild": "^0.28.2",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  }
}
